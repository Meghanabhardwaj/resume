[project]
name = "resume-editor"
version = "0.1.0"
description = "Local Flask UI for editing, generating, previewing, and pushing the resume."
requires-python = ">=3.12"
dependencies = [
  "Flask==3.1.0",
  "PyYAML==6.0.2",
  "jsonschema==4.23.0",
]

[tool.uv]
package = false
